import 'package:flutter/material.dart';

class CueingScreen extends StatelessWidget {
  const CueingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Adaptive Cueing")),
      body: const Center(
        child: Text("Semantic Hierarchy Module - Coming Soon"),
      ),
    );
  }
}
