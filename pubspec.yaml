name: verbal_bridge
description: AI-Based Adaptive Speech Regeneration App for Persons with Aphasia.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.0
  google_fonts: ^6.1.0
  audioplayers: ^5.2.1
  record: ^5.0.0
  firebase_core: ^2.24.0
  cloud_firestore: ^4.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
